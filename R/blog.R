#' 
#' @export
new_post <- function(title, serve = TRUE, dir = "~/Project/Notebook/Labnotebook",
                     subdir = TRUE, skeleton_file = ".skeleton_post"){
 
  # create Rmkdown with blog template if do not exist
  
  # open it in R studio
  file.edit()
  
  # compile with jekkyl and display in viewer
  
  
}

NormalizeName(file.name){
  
}


BuildPost <- function(file.name,jekyll.dir = "~/Project/PersonnalWebsite"){
  # build md with knitr
  
  # copy in place
  
  # build with jekyl 
  
  # view in rstudio viewer
  
}

PublishPost <- function(){
  
}

